How to call isAuthorized:
 It receives an isAuthorizedRequest which takes in three parameters a userID, theServiceRestrictionObject 
 and buzzSpaceObject to get the moduleid for GetUsersRolesForModuleRequest.
 buzzSpace class receives academicYear , isOpen a boolean, moduleid e.g COS330.
 var isAuthorizedRequest = function(userID, theServiceRestrictionObject, buzzSpaceObject)
 {
   var userid;
   var serviceIdentifierOject;
   var BuzzSpaceObject;
   
   this.userid = userID;
   this.serviceIdentifierOject = theServiceRestrictionObject;
   this.BuzzSpaceObject = buzzSpaceObject;
 };

Then this isAuthorizedRequest object is passed in isAuthorized.
